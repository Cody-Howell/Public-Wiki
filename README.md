# Public Wiki

This creates a C# API with a React frontend, that uses some of the libraries I've built for authentication and display.

All data is stored in the Postgres instance, with (authenticated) API calls to return a SQL dump of the current record for backups. 

This version is currently hosting my wiki present at: (to be determined once set up).
